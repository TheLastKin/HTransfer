import React from 'react';

type QuickActionProps = {
  onQuickAction: () => void,
  action: string,
  tooltipText: string
}

const QuickAction = () => {
  return (
    <div>

    </div>
  );
};

export default QuickAction;
